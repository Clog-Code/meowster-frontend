// ignore_for_file: avoid_print

void bootstrap() {
  // This file is selected only by the standalone Dart VM. Flutter builds use
  // bootstrap_flutter.dart via the dart.library.ui conditional import.
  print(
    'This is a Flutter app. Run it with:\n'
    '  flutter run --dart-define=AGENT_BASE_URL=http://localhost:8000\n\n'
    'In VS Code, use the Flutter launch profile instead of Code Runner.',
  );
}
